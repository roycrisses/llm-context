## 2026-04-15 - [Default CLI Directory]
**Learning:** Defaulting the directory argument to `.` in CLI tools significantly improves UX for common local-run scenarios, reducing typing effort and making the tool feel more "plug-and-play".
**Action:** Always consider if a positional directory/path argument can have a sensible default (usually current directory) to streamline user interaction.
