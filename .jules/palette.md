## 2025-05-15 - [CLI Defaults and Feedback]
**Learning:** For terminal-based tools, sensible defaults (like current directory) significantly reduce friction. Providing immediate feedback on critical metrics (like token counts) helps users validate the output without extra steps.
**Action:** Always look for mandatory CLI arguments that could have sensible defaults, and identify "success" metrics to include in completion messages.
